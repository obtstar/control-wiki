schema_version: "1"
server:
  host: "127.0.0.1"
  port: 8766
  api_key: ""

distill:
  base_url: "http://litellm.internal:4000/v1"
  token: ""          # 经 PIEKBS_DISTILL_TOKEN 注入，不落盘；piekbs 要求非空，免鉴权网关可填占位值
  model: "cheap"
  api_type: "openai"
  workers: 3

embedding:
  idle_timeout: "5m0s"

ui:
  language: "en"
